use std::{fs::File, io::{BufRead, BufReader}};


fn main() {
    // let trace = BufReader::new(File::open("trace.txt").unwrap()).lines();
    // for line in trace {
    //     let line = line.unwrap();
    //     let (first, second) = line.split_once(" ").unwrap();
    //     let key: i128 = first.parse().unwrap();
    //     let op: u8 = second.parse().unwrap();

    //     dbg!(key, op);
    // }
}
